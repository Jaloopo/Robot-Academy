# Stora binärer – idébank, committas inte
docs/reference/*.pdf
# Handoff-/exportarkiv ska inte committas
*.zip

# OS-skräp
.DS_Store

# Dev-testverktyg (jsdom) – beroenden installeras lokalt, committas inte.
# Sajten själv är fortsatt beroendefri; node_modules ingår aldrig i file://-/Pages-bygget.
node_modules/

# Visual-QA-screenshots från npm run snap (dev-only)
artifacts/
